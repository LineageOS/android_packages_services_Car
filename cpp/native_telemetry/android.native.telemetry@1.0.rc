service nativetelemetry_service /system/bin/android.native.telemetry@1.0
    class core
    user system
    group system
    disabled  # starts below on early-init

on early-init
    # Start the service only after initializing the properties.
    start nativetelemetry_service